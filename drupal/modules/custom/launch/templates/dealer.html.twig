{#
/**
 * @file dealer.html.twig
 * Default theme implementation to present Dealer data.
 *
 * This template is used when viewing Dealer pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_dealer()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('dealer') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
